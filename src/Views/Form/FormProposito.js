import React from 'react';
//import { List, ListItem, ListItemContent } from 'react-mdl';
import { withRouter } from 'react-router';

class FormProposito extends React.Component {
    render() {
        return (
            <p>proposito</p>
        );
    }
}

export default withRouter(FormProposito);